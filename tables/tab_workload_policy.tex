% Table: draft workload taxonomy (Chapter 3 model).
% Referenced as Table~\ref{tab:workload_policy}.
%
% Source: WorkloadKey.kt in the private ML implementation (commit 5480cd5):
% sealed WorkloadKey types, their key context, and WorkloadPolicy
% (OPTIONAL / REQUIRED / RESERVED). Rows are in draft-path order; Decoding
% appears only on JPEG inputs, before the YUV effects.
\begin{table}[t]
  \centering
  \caption{Draft workload taxonomy. Each node execution is classified into
  a workload key; the key's policy fixes its role under control.}
  \label{tab:workload_policy}
  \resizebox{\columnwidth}{!}{%
  \begin{tabular}{llll}
  \toprule
  \textbf{Workload} & \textbf{Key context} & \textbf{Policy} &
  \textbf{Role under control} \\
  \midrule
  Multi-frame stage (M) & size bucket & optional & admission-gated, watchdog-guarded \\
  Dynamic Function & size bucket & required & always runs; observed only \\
  Decoding & size bucket & optional & JPEG-to-YUV prerequisite of
  YUV effects \\
  Single-frame stage (S) & size bucket & optional & admission-gated, watchdog-guarded \\
  Watermark (overlay) & size bucket, type & optional & degradable visual
  enhancement \\
  Watermark (frame) & size bucket, type & required & correctness stamp;
  forces Decoding \\
  Encoding & size bucket, format, request & reserved & mandatory tail;
  budget-protected \\
  \bottomrule
  \end{tabular}%
  }
\end{table}
