% Requires TikZ libraries: positioning, arrows.meta, fit, calc.
\begin{figure}[t]
  \centering
  \resizebox{\columnwidth}{!}{%
  \begin{tikzpicture}[
      font=\scriptsize,
      >=Latex,
      node distance=4mm and 8mm,
      stage/.style={draw, rounded corners=1.2pt, align=center, minimum height=7mm, text width=25mm, fill=blue!5},
      draft/.style={stage, fill=green!8},
      final/.style={stage, fill=orange!8},
      result/.style={draw, align=center, minimum height=7mm, text width=25mm, fill=gray!7},
      arrow/.style={->, line width=0.45pt}
    ]
    \node[stage] (request) {Capture request};
    \node[stage, right=7mm of request] (inputs) {Required frames\\and metadata};

    \node[draft, above right=5mm and 8mm of inputs] (draftseq) {Draft Sequence\\representative frame};
    \node[draft, right=6mm of draftseq] (publish) {Encode, store, and\\database publication};
    \node[result, right=6mm of publish] (early) {Early durable result\\and recovery artifact};

    \node[final, below right=5mm and 8mm of inputs] (post) {Full multi-frame\\post-processing};
    \node[result, right=37mm of post] (final) {Final photograph};

    \draw[arrow] (request) -- (inputs);
    \draw[arrow] (inputs.east) -- ++(4mm,0) |- (draftseq.west);
    \draw[arrow] (inputs.east) -- ++(4mm,0) |- (post.west);
    \draw[arrow] (draftseq) -- (publish);
    \draw[arrow] (publish) -- (early);
    \draw[arrow] (post) -- node[below] {longer path} (final);
    \draw[arrow, dashed] (final.north) -- (early.south);
  \end{tikzpicture}%
  }
  \caption{The Draft Sequence publishes an early durable image while full post-processing produces the final photograph. The Draft also remains available as a recovery artifact if final processing fails.}
  \label{fig:draft-pipeline}
\end{figure}
