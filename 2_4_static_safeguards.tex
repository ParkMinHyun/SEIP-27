\subsection{Limitations of Static Thermal Gating}
\label{sec:guard-limit}

\smallskip\noindent\textbf{Production thermal guard.}
Samsung's overheat level is an ordinal thermal indicator on a 0--6 scale, where larger values denote stronger throttling.
During pre-release validation, level-4 throttling delayed a single-frame Draft stage and caused a Capture Timeout.
After the failure recurred despite optimization, we adopted a uniform safeguard that skips optional Draft processing at level 4 or higher.
Because the Draft Sequence is implemented in framework code shared across models, the safeguard applies uniformly rather than through model-specific branches.
However, level 4 is not confined to stress testing; it can also arise under high ambient temperatures, charging, or preceding device workloads.
Nor does the level reveal how much of a capture's deadline has already been consumed by queueing or how much Draft work remains.

\smallskip\noindent\textbf{Target workload and configuration.}
We denote the candidate lightweight multi-frame Draft stage by \emph{M} and the deployed all optional single-frame stages in the target Portrait configuration by \emph{S}. 
We compare M with M+S; both are followed by the same mandatory encoding and saving.
To reduce the draft--final visual gap in Section~\ref{sec:draft-background}, we aim to make M available across all supported models and levels and execute it whenever safely possible, rather than disabling it uniformly at level 4.
We first evaluated M on the Galaxy S26 Ultra, the highest-performing model, to determine whether the problem persists even on the most capable device.
With M enabled and the guard bypassed, we ran ten 30-capture trials per combination of starting level (0--6), resolution, memory condition, and Draft Sequence configuration.
The motivating level-4 failure occurred near capture 20 with 12 Draft Sequences pending, so each trial continued to 30 captures to exercise the pipeline beyond the observed failure point.
Memory-pressure tests ran the camera alongside AnTuTu Benchmark~11.1.4's memory-stress workload.
Table~\ref{tab:timeout_index} reports the earliest and median first-timeout indices for each starting level.

\input{tables/tab_timeout_index.tex}

\smallskip\noindent\textbf{Required runtime control.}
Some trials starting at overheat levels 0--3 timed out even though the production guard permits optional Draft processing at those levels.
Conversely, at starting levels 4--6, several captures completed optional Draft processing before the earliest timeout, even though the guard would suppress it from the first capture.
Together, these results show that a fixed thermal threshold cannot reliably identify which captures have sufficient budget to execute \(M\).
Neither executing \(M\) unconditionally nor adjusting only the current capture's work resolves this mismatch: the former requires spacing subsequent captures farther apart to prevent queued work from consuming their budgets, whereas the latter can neither recover budget already lost while waiting for the Draft worker nor account for a throttling-induced slowdown after the workload has been fixed.
Reliable multi-frame composition therefore requires coordinated runtime control that scales each capture's Draft work to its remaining budget and delays the next capture only as much as the current backlog requires.

\input{tables/tab_rq1_end_to_end_summary.tex}
\input{tables/tab_rq1_ablation.tex}
\input{tables/tab_rq2_admission_summary.tex}
\input{figures/fig_rq2_unsafe_spike_anatomy.tex}
% Provisional placement: this paragraph belongs in the experiments section once
% that section is written, next to the RQ3 results it sets up.  It is parked
% here because the RQ3 float pair is parked here.  Without it the table and the
% figure present d* with no stated justification, which reads as the controller
% grading its own homework; the point of the paragraph is that d* is the
% deployed condition with measured inputs, so what it grades is the estimator,
% not the policy.
\smallskip\noindent\textbf{What the pacing delay is measured against.}
The controller admits the next capture only when the work already committed to
the Draft worker still leaves the deadline reachable.
Writing \(\hat{B}\) for the estimated backlog, \(\hat{C}\) for the reserved
Draft duration, and \(T\) for the window remaining on the newest committed
capture's deadline, it charges a delay of
\(d=\lceil\max(0,\hat{B}+2\hat{C}-T)/2\rceil\).
Two Draft reserves appear because a pacing decision has to cover both the Draft
that starts once the queue drains and the Draft belonging to the capture the
delay itself releases.
The halving is what makes the delay self-correcting: one millisecond of delay
both drains one millisecond of backlog and moves the released capture's deadline
one millisecond later, so it closes twice its own length of deficit.
Evaluating that same condition on measured rather than estimated inputs --- the
reconstructed backlog and the Draft duration as it actually ran --- yields the
requirement \(d^{*}\) that Table~\ref{tab:rq3_pacing_calibration} and
Figure~\ref{fig:rq3_pacing_calibration} compare the applied delay against.
Because \(d\) and \(d^{*}\) share the horizon, the deadline, and the arithmetic,
their difference isolates the cost of mispricing the inputs rather than any
property of the policy, and it decomposes exactly into the two estimators that
produced them.
What that comparison asks is whether the applied delay \emph{covered} the
requirement, not whether it equalled it.
The reserve \(\hat{C}\) is the burst's observed maximum Draft duration rather
than a prediction of the Draft that follows, so \(d\) holds an envelope over
Draft cost instead of tracking any single transition's need: fitted against the
measured requirement it moves by roughly a third of it, over a floor of a few
hundred milliseconds.
That is the intended trade for a hard deadline, where under-provisioning costs a
user-visible failure and over-provisioning costs bounded delay, and it is what
the surplus reported below prices.
The comparison is also trace-conditioned: \(d^{*}\) is computed on a trace that
pacing already shaped, so it bounds how much delay could have been removed from
above and how often the delay fell short from below.
We therefore call a pacing delay appropriately sized on three conditions: that
it never falls below what the mandatory Draft workload alone requires, that it
covers the requirement of the configuration actually executed on the large
majority of transitions, and that its cost is bounded against the Capture
Timeout budget and attributable to a named estimator rather than to the policy.
Minimality is deliberately not among them.
A trace-conditioned comparison cannot establish it, and the evidence that the
delay is needed at all is the ablation of Table~\ref{tab:rq1_ablation} rather
than anything in this section.

\input{tables/tab_rq3_pacing_calibration.tex}
\input{figures/fig_rq3_pacing_calibration.tex}
\input{figures/fig_casestudy_12mp.tex}
\input{tables/tab_casestudy_selection.tex}